# Docker 环境变量模板
# 使用方法: cp env.docker.template .env.docker.prod

# 数据库（使用宿主机 PostgreSQL）
DATABASE_URL="postgresql://postgres:password@host.docker.internal:5432/cf_landing?schema=public"

NODE_ENV=production
